module.exports = {
  config: {
    name: "mentionreply",
    author: "SaGor",
    role: 0,
    shortDescription: "Specific reply for specific IDs",
    longDescription: "",
    category: "BOT",
    guide: "{pn}"
  },

  onChat: function ({ api, event }) {

    if (!event.mentions || Object.keys(event.mentions).length === 0) return;

    // ৬টা আইডি + মেসেজ
    const data = {
      "100057683829700": "আনিকা অভিমানী অসুস্থ, দোয়া করো যাতে দ্রুত সুস্থ হয়ে যায়, সুস্থ হলে সবার সাথে কথা বলবে💗🤗",
      "100073298735758": "বিবাহিত মহিলা কেউ নজর দিবা না",
      "61588703220029": "সাথী ম্যাম এখন ব্যস্ত আছে গ্রুপে আসলে কথা বলবে",
      "100018836995800": "🌟 ভাই, আপনি একটি নির্দিষ্ট আইডিকে মেনশন দিয়েছেন!",
      "100912345678900": "💎 এই আইডির জন্য আলাদা মেসেজ সেট করা আছে!",
      "100698765432110": "⚡ এই আইডি মেনশন হলে সবসময় এই মেসেজটি যাবে!"
    };

    // উল্লেখিত ID গুলো
    const mentionedIDs = Object.keys(event.mentions);

    // মেনশন করা ID গুলোর একটার জন্যও যদি মেসেজ থাকে → পাঠাবে
    for (const id of mentionedIDs) {
      if (data[id]) {
        return api.sendMessage(
          data[id],
          event.threadID,
          event.messageID
        );
      }
    }
  },

  onStart: async function () {}
};
